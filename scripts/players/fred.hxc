import funkin.ui.freeplay.charselect.PlayableCharacter;
import funkin.save.Save;

class FredPlayableCharacter extends PlayableCharacter
{
	function new()
	{
		super('fred');
	}

  override function isUnlocked():Bool {
    return Save.instance.hasBeatenLevel('vof');
  }
}